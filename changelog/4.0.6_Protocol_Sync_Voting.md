# Skoolchein Protocol v4.0.6 — Protocol Sync (Voting & Staking)

## 🗳️ Governance Restoration
Restoring the missing voting and staking functions that were hidden during v4.0.3 bytecode optimization. This release synchronizes the `ScholarshipCore.sol` with the frontend logic to support industrial-scale community participation.

- [x] **Dual-Mode Participation (Phase 6.3)**:
    - [x] Restore `voteForCandidate` as an external entry point in `ScholarshipCore.sol`.
    - [x] **Mode 0 (Donation)**: Implement logic to spend `remainingVotingPower` (USDC-based).
    - [x] **Mode 1 (Reputation)**: Implement logic to use global `reputation.balanceOf` (SREP-based).
    - [x] Restore `placeConfidenceStake` and ensure it respects the `openDonation` master switch.

- [x] **UI Visibility Logic (Phase 6.4)**:
    - [x] Implement `openDonation` gating: hide "Vote" and "Donate" buttons if `false`.
    - [x] Update `ProgramDetailsPage.tsx` to react to the `openDonation` state change.

- [x] **Indexer Synchronization (Phase 6.5)**:
    - [x] Update **Ponder** handlers to track restored `VoteCast` and `ConfidenceStaked` events.
    - [x] Sync the `contractsV4` ABI constants in the frontend.


## 🚦 Security Standards
All voting functions now strictly enforce:
1. `VOTING` status check.
2. `openDonation` master switch.
3. Candidate `SHORTLISTED` status.

## 🟢 Phase 3: Advanced Integrations (ON DECK)
- [ ] **Udemy/Dicoding Webhooks**: Implement platform-specific parsing in the controller.
- [ ] **Sertifikat Verification**: IPFS verification of completion certificates.

---
*Status: COMPLETED (v4.0.6-stable)*
